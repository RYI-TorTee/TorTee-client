import React from "react";
import './Footer.scss'

function Footer({ backgroundColor }) {
    return (
        <div className="footer-container" style={{ backgroundColor: backgroundColor }}>
            hihi
        </div>
    );
}

export default Footer;